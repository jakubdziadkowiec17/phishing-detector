DATASET = "ndarvind/phiusiil-phishing-url-dataset"

# If set, kagglehub will load a specific file path inside the dataset.
# Otherwise we download to `data/kagglehub` and pick the first CSV found.
FILE_PATH = ""

DEFAULT_LABEL_COL = "label"
DEFAULT_URL_COL = "URL"

